const Navbar = () => {
  return (
    <header className="flex items-center justify-between h-16 px-4 md:px-8 lg:px-12 py-4 border-b border-primary/30">
      <img src="/logo.png" alt="Zapis Logo" height={36} width={36} />
    </header>
  );
};

export default Navbar;
